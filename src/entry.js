import React, { AppRegistry } from 'react-native'; // eslint-disable-line no-unused-vars
import App from './components/App';

export default function native() {
  AppRegistry.registerComponent('onehundredthings', () => App);
}
